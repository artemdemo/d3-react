import React, { Component } from 'react';

export class ToolTip extends Component {
    render() {
        return null;
    }
}

ToolTip.propTypes = {
    position: React.PropTypes.string, // `top`, `bottom`, `left`, `right`
    $$rootGroup: React.PropTypes.object,
};
